# E-Commerce Backend API

This is the backend server for the High-Performance E-Commerce Engine.

## Technologies
- Node.js & Express
- MongoDB (with local memory fallback for dev)
- Redis for caching
- Hugging Face Transformers for local vector embeddings

## Running the Server
Use `npm run dev` in the root workspace to run this concurrently with the frontend.
